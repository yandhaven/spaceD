import React from 'react'
import modules from '../Header/Header.module.css'

const Header =()=>{
    return <header className={modules.header}>
        <img src='emblemURL' />
    </header>
}

export default Header;